import styled, { css } from 'styled-components'

const HeaderText = styled.h2`
  font-size: 3rem;
  margin: 0 0 0.4em 0;


  /* ${props =>
    props.primary &&
    css`
      background: #f37021;
      color: white;
    `}; */
`

export default HeaderText;